# journal — 케이지 작업 일지

[cage.md](cage.md)는 **지금 무엇인가**의 선언이고, 이 문서는 **어떻게 여기까지 왔나**의 기록이다. 세션마다 항목 하나: 시작 상태, 관찰, 결정과 그 이유, 확정된 수치, 남긴 일. cage.md와 어긋나면 cage.md가 맞다.

---

## 2026-08-29 — 쇄골에서 머리까지

커밋: `168c699` → `e12d603` → `5c31219` → `77974a1`

### 시작 상태
- 링 10개(crown, L/R arm·elbow·wrist, hip, knee, sole) + 손. 176 정점 / 348 삼각형.
- 쇄골~머리 구간의 제어점은 crown 캡과 arm 링뿐. 몸통 8각형의 윗변이 `crown·hi → arm·hi` 대각선 하나.
- **arm 링 윗변(y 0.624)이 Head 관절 높이(0.622)와 같았다** — 어깨 살 실측 8.1 cm + `hi` 여유 5 cm가 겹쳐 어깨 링이 턱 높이까지 올라가 있었다. 목(0.49~0.62)은 그 대각선 벽과 깊이 0.32→0.49로 벌어지는 앞뒤판 사이 공기층에 놓임.
- 목 길이 편집 → 움직이는 제어점은 crown 4개뿐 → 얼굴이 목과 같은 비율로 늘어남.

### 작업 방식 (이 세션에서 정한 것)
- **임시 편집기로 값을 찾고, 확정되면 표로 옮긴다.** `cage_tune`(mapping_tester에 직렬화) 필드 하나 = 인스펙터 슬라이더 하나. 드래그 중에는 재bake + 케이지 갱신만(ms), 놓으면 재bind + deform(초). 확정된 값은 cage.md §3 표와 recipe 리터럴로 옮기고 슬라이더는 지운다 — 이 세션에서는 아직 옮기지 않았다(전부 "튠 중").
- Unity를 밖에서 돌릴 수 없어 컴파일은 `dotnet build unity/Assembly-CSharp.csproj`(임시 출력 경로), 토폴로지는 표를 파이썬으로 재현해 닫힘·Euler 검사. containment·deform은 에디터에서 사람이 확인.

### 진행

**1. arm 링 `hi` 튠 (`168c699`)** — 첫 슬라이더. 어깨 링 윗변을 살 + margin까지 내림(hi −0.0005).

**2. 정중선 (`e12d603`)** — 한쪽 겨드랑이를 당기면 반대편이 끌려오는 비국소성과, 앞으로 넣을 V넥 모두 정중선 정점을 요구했다. 국소로는 불가능: 판 변 위의 정점은 그 변을 공유하는 양쪽 판에 다 들어가야 닫힘 assertion을 통과하므로, 정중선은 crown → hip → knee → sole을 관통하는 사슬이 된다.
- 프리미티브는 6점 링이 아니라 **정중선 기둥(post)**. 손의 `cage_post`가 이미 "아핀 앵커 + d 축 정점 2"였고, 링 번호 규칙·토폴로지 표(링 변과 기둥을 섞어 씀)·옆판이 그대로 남는다.
- 기둥의 `center` 하나를 **d 앵커 두 세트**로 일반화 — 링 변과 같은 규칙(max/min + 여유). sole 앞변은 ToeBase가 정하므로 중심 관절 하나로는 발 길이 편집 때 mid가 변에서 떨어졌다.
- 몸통·허벅지·종아리 판을 좌/우 반판으로, crown·sole 캡을 쿼드 2개로. 정점은 그대로지만 비평면 판의 삼각화가 바뀌어 가슴·배 표면 깊이가 crown↔hip 보간으로 바뀌었다.
- 그 결과 arm 링의 front 0.2 / back 0.1과 crown front 0.1은 가슴을 덮지 못하고 옆만 부풀렸다 → 걷어내고 측정값 + margin으로. 날개뼈·가슴~배꼽 정중선이 새는 것은 crown/hip의 `front`/`back` 튠으로 받았다.
- 비국소성: 반대편으로 어느 정도 전이되지만 자연스러운 수준. MVC 가중치가 전역이라 완화일 뿐 — 해결은 좌표계(Green/Somigliana) 쪽.

**3. 라글란 arm 링 + V넥 (`5c31219`)** — arm 링 윗변을 몸통 안쪽·위로 들여 승모근 위에 얹고, 아랫변을 겨드랑이 속으로. 팔 판이 라글란 소매가 되어 삼각근이 소매 안에 들어가고, 두 윗변과 Neck 위의 `neck mid`가 앞뒤로 V를 이룬다.
- 기존 몸통 반판(crown·hi–hip·hi 현이 세로 가로대)은 승모근 점이 현 안쪽에 들어와 **접힌다**(닫힘은 유지, 표면이 겹침). 판을 V에서 잘라 몸통 반판은 `arm·hi → arm·lo → hip·hi → hip·mid → sternum → neck`, 머리 반판은 V → crown. 양쪽 볼록.
- 가로대를 가로로 눕히려면 실루엣 점마다 정중선 점이 필요 → 겨드랑이 높이에 `sternum mid`(Spine3, y 0.402 ≈ 겨드랑이 0.40). 5각형은 ladder가 못 채운다.
- 링은 변별 `along`(outward hi/lo)을 얻었고, 이어서 변별 s 여유(`lo` 신설)와 **코너별** 깊이 여유(`hi_front, hi_back, lo_front, lo_back`)까지. 두 변이 `d`에 평행이라 네 코너는 여전히 한 평면 — N1의 "깊이 공유라 평면" 근거는 "변 평행이라 평면"으로 고쳤다.
- neck/sternum mid는 자기 가로대의 arm 변 깊이를 따른다 → V–겨드랑이 가슴 띠의 앞뒤는 arm 링 hi/lo front/back으로 조절.
- 승모근 점은 어깨 관절에 고정 오프셋 → 쇄골 편집을 100% 따라가는 근사. 절반이어야 하면 아핀 기둥으로.

**4. 머리–목 분리 링 (`77974a1`)** — 턱끝이 Head 관절보다 앞·아래에 있어 분리 평면은 기울어져야 한다. 씬에 놓아둔 **head splitter** 평면을 Hips 공간으로 환산: 타깃 rig root 월드 (2,0,0)·−90° x·×100, Hips 로컬 +90° x → Hips 공간 = 월드 축. 평면 법선 (0, .906, .423) = `side` 축 25°, Head에서 법선 방향 0.023 m.
- `head` 링: `n = up↷25°`, `d = depth↷25°`, `s = side`, outward 0.023. 링 코드는 프레임이 직교면 되므로 기울어진 링도 같은 코드.
- 측정 종류 **split** 신설(`bool terminal` → `enum fit{ joint, cap, split }`): 평면은 앵커 + outward에 고정, 단면은 평면 너머 Head 살 전체 — 머리 판이 감싸야 할 것. crown과 비슷한 값이 나오지만 종속은 아니다.
- 판: 목 반판(V → head), 머리 반판(head → crown). 목 길이 편집이 목 판만 늘이고 머리는 rigid하게 오른다 — 에디터에서 확인.

### 씬에 저장된 튠 값 (2026-08-29 종료 시점, 씬 단위 m)

| 대상 | 값 |
|---|---|
| arm `hi` / `lo` | 0.002 / 0 |
| arm `outward hi` / `outward lo` | −0.05 / 0.02 |
| arm hi `front` / `back` | 0.03 / 0.02 |
| arm lo `front` / `back` | 0.055 / 0.02 |
| head `tilt` / `offset` | 25° / 0.023 |
| head `front` / `back` | 0.005 / −0.04 |
| crown `front` / `back` | 0.01 / 0.004 |
| hip `front` / `back` | 0.02 / 0.004 |

종료 시 containment 통과. 194 정점 / 384 삼각형.

### 남긴 일
- 튠 값을 cage.md §3 표와 recipe로 옮기고 슬라이더 정리. head splitter 오브젝트 삭제.
- 승모근 능선에 딱 맞추는 변별 측정 창(지금은 어깨 관절 평면에서 잰 삼각근 정점 높이).
- `neck mid`가 Neck 관절 자리라 V가 얕다(arm·hi와 2 cm) — 내리는 여유가 필요할 수 있다.
- 두께 driver, 척추 세부 링(정중선 사슬에 가로대 하나씩), 자기겹침 스윕 — cage.md §9.

---

## 2026-08-30 — 골반에서 발끝까지

커밋: `1eee5a8` → `a982e22` → `954124f`(씬) → `53f226d` → `bd5a6e7` → `f7c4923`

### 시작 상태
- 링 11 + 정중선 기둥 7 + 손, 194 정점 / 384 삼각형. 쇄골~머리 완료.
- 다리는 **스커트 프리즘 하나**: `hip`·`knee`·`sole` 링이 양다리 공용이고 정중선 기둥이 앞뒤 판을 좌/우로 가를 뿐, 다리 사이 안쪽 벽이 없다. 가랑이·안쪽 허벅지가 공기층. 공용 링은 한 다리 편집에 반대 다리를 끌어간다(`[N1]`의 대가).
- 발은 `sole` 캡 하나 — 종아리 프리즘의 바닥면. 발등·뒤꿈치·발가락이 전부 밖.
- 지난 세션 저널의 `hip front` 0.02는 씬에서는 0.04로 저장돼 있었다(저널 뒤에 더 튠). 이 세션에서 hip 링이 없어져 값 자체는 소멸.

### 진행

**1. 골반 분기 (`1eee5a8`)** — 손의 §4 구조를 골반에.
- `hip` 링 → **`spine` 링**(Spine 관절, joint, 허리). 몸통 반판의 아랫변이 `spine·hi → spine·mid`.
- 골반은 링이 아니라 **기둥 3**: `crotch`(Hips − up·drop), `L/R hip`(앵커 (UpLeg, Hips) 가중치 (1+f, −f) + up·f·drop = `UpLeg + f·(UpLeg − crotch)`). 손가락 endbone과 같은 수법이라 고관절 폭 편집에 (1+f)배로 따라 나간다. 셋이 골반 살(Hips·양 UpLeg 본)의 depth 구간 하나를 공유(d 앵커 Hips) — 손의 판 두께처럼.
- 왜 링이 아니라 기둥인가: 두 고관절 링이 가랑이에서 **만나야** 하는데 링(정점 4)은 정점을 공유할 수 없다. 손가락 분기 링이 이웃한 손바닥 기둥 둘인 것과 같이 `crotch – L hip`이 왼다리의 기울어진 고관절 링. 앞에서 보면 두 링이 V, spine 링과 함께 손등 같은 5각형 → 정중선 규약대로 `spine·mid – crotch`에서 반판 둘.
- 토폴로지 표에서 역 `hip`의 `hi`/`mid`/`lo`가 기둥. 정중선 기둥 헬퍼를 `at[(역, 변)]` 사전 + 가중치 있는 `post()`로 일반화 — 이후 발끝 기둥도 이 사전을 그대로 쓴다.
- `knee`/`sole` → `L/R knee`, `L/R sole` 단일 사지 링(자기 다리 살만). 정중선 사슬은 crotch에서 끝나고 그 아래 두 다리는 각자 닫힌 관 — `[N10]` 갱신, `[N1]`은 역사로.
- 튠 신설: `crotch drop`, `hip out`(비율), `pelvis front/back`, `spine front/back`(hip 것 대체), `knee out`(양 링 바깥 변)·`knee back`(고정 0.1 → 슬라이더). 204 정점 / 404 삼각형.

**2. 발 (`a982e22`)** — sole 삭제, 다리별 `knee → ankle → toe → tip`.
- **ankle**: Foot 관절을 지나되 **뒤로 기울어진** 링(`n = −up↷tilt`, `d = depth↷tilt`, 초기 45°). 수평이면 뒤꿈치 아래와 발등 위를 동시에 자르지만, 뒤꿈치 바닥에서 발등–정강이 연결부로 기울이면 종아리 관과 발 관을 가르는 자연스러운 단면. wrap = LeftLeg 서브트리, joint slab.
- **toe**: ToeBase에서 `n = depth, d = up` 세로 링 — front = 발등, back = 발바닥.
- **tip**: 관절 없는 endbone 위의 **기둥 쌍**(`(1+f, −f)`·(ToeBase, Foot), f = 발가락 살이 ToeBase 너머로 뻗은 길이 ÷ 발 뼈) — 발 길이에 비례해 따라 나가는 뚜껑. 캡 = `tip·hi → tip·lo` 쿼드 1.
- 프레임의 `d`가 knee(depth) → ankle(depth↷45°) → toe·tip(up)으로 연속해서 돌아 **앞판 = 정강이 → 발등 → 발가락 위, 뒷판 = 종아리 → 뒤꿈치 → 발바닥**. 링 코드는 그대로(직교 프레임이면 됨). `[N14]`.
- **평평한 발바닥**: 링에도 기둥처럼 **변별 깊이 앵커**(`d_hi_anchor`/`d_lo_anchor`, 기본 = 앵커 전체)를 두고, toe 링의 아랫변과 tip의 아랫끝을 **Foot에 걸어 ankle 링 바닥(뒤꿈치) 높이**에 둔다. 위쪽만 살에서 재고, ankle `back` 하나가 발 전체의 바닥을 정한다. 발 뼈를 늘여도 발바닥은 뒤꿈치와 한 평면.
- 튠: `ankle tilt`, `ankle front`(발등 쪽, −0.1까지)·`back`(뒤꿈치 쪽). 220 정점 / 436 삼각형.

**3. 씬 (`954124f`)** — 옛 constants(링에 d 앵커 없음)는 `rebuild cage` 전에 `ring_corners`에서 예외 — 의도된 fail-loud, 방어 코드 없음.

**4. 척추 세부 링 (`53f226d`)** — Spine(허리)과 Spine3(sternum mid) 사이에 가로대가 없어 배·아랫가슴 깊이가 보간이었다. Spine1·Spine2에 `spine`과 같은 가로 joint 링 + 정중선 기둥 하나씩.
- 몸통 반판 10점: `arm·hi → arm·lo → spine2·hi → spine1·hi → spine·hi → spine·mid → spine1·mid → spine2·mid → sternum → neck` — 가로대가 sternum·spine2·spine1·spine 관절마다 하나. 정중선 사슬은 crown → head → neck → sternum → spine2 → spine1 → spine → crotch. 척추 마디 길이 편집이 그 구간만 늘인다.
- 튠 `spine1/2 front/back`(초기 0). 232 정점 / 460 삼각형. 슬롯 번호가 바뀌어 `rebuild cage` 필수.
- 확인할 것: Spine2 관절이 겨드랑이(arm·lo ≈ Spine3 높이) 아래인지 — 위라면 spine2·hi–arm·lo 옆판이 접힌다.

**5. 어깨–상완 분리 (`bd5a6e7`)** — arm 링 윗변이 승모근 위로 들어간 뒤(N11) 상완 판 윗변이 승모근 점 → 팔꿈치 위 직선이 되어, 앞에서 보면 삼각근 너머 상완 위에 빈 공간이 컸다.
- 골반(N13)과 같은 답: 어깨와 상완을 가르는 링은 **겨드랑이 정점(arm·lo, R쪽 v18–v19)을 arm 링과 공유**해야 하므로 링(정점 4)이 아니라 **기둥 하나 `delt`** + arm·lo 변. 상완 위 삼각근 끝에서 겨드랑이로 내려오는 기울어진 세로 링이고, 앞에서 arm 링과 겨드랑이에서 만나는 V.
- 그 사이 어깨 쐐기 = 앞/뒤 **삼각형 판**(arm·hi – delt – arm·lo) + 위쪽 벽 쿼드(arm·hi → delt). `strip`이 홀수 고리를 삼각형 하나로 끝내도록 넓혔다(짝수 고리 결과는 불변 — 옛 표 194/384 재현으로 확인). 상완 판은 `delt → elbow·hi → elbow·lo → arm·lo`.
- `delt` 위치: 앵커 (Arm, ForeArm) 가중치 (1−g, g), `g = delt along`(초기 0.4) — 상완을 늘이면 비율로 미끄러진다. 그 지점 평면 ±slab 안의 LeftArm 살에서 up 위끝(+`delt up`)과 depth 앞/뒤를 잰다. 살 창이 LeftArm 본만 보므로 삼각근이 LeftShoulder 본에 지배되면 낮게 잡힐 수 있다.
- 같은 커밋에 elbow 링 `hi`(고정 0.05) → 슬라이더. 씬에서 0.01로 내려왔다. 236 정점 / 468 삼각형.

**6. 좌우 거울 대칭 삼각화 (`f7c4923`)** — 골반·허벅지에서 좌우 토폴로지가 다르게 보였다. 조사 결과 정점과 ladder 가로대는 이미 대칭이고 **쿼드 대각선만** 달랐다: ladder는 대각선을 항상 `v[i]–v[j−1]`로 긋는데, 오른쪽 판은 왼쪽 판의 거울상을 **역순**으로 추적하므로(거울은 방향을 뒤집어 winding을 지키려면 역순) 다른 코너를 잇는다. 뒷면도 앞면 고리의 역순이라 같은 일이 생겨, 케이지는 거울 대칭이 아니라 **up 축 180° 회전 대칭**(왼 앞판 ≡ 오른 뒷판의 거울)이었다. 비평면 쿼드에서는 표면이 달라 대칭 메시의 containment·MVC 가중치가 좌우 다르게 나온다.
- 답: `strip(loop, flip)` — `flip = 거울면 XOR 뒷면`이면 반대 대각선. 좌우가 정확한 거울이 되고, 쿼드가 앞/뒤 같은 두 제어점을 잇는 선으로 접힌다. 거울면 판정은 면의 rest 중심 `side` 부호(정중선에 걸치는 면은 assertion — N10 덕에 없다). 표·recipe 변경 없음. 손도 같은 규칙(`mirror` 역순 추적이 같은 문제를 갖고 있었다).
- 검증: 파이썬으로 몸통 192면의 삼각형 집합이 정점 거울 매핑(+winding 반전) 아래 닫힘(차집합 0); 기존 규칙은 차집합 376.
- 대안으로 "오른쪽을 왼쪽 표에서 생성"(표 절반, 구성상 대칭)을 검토했으나 §5 표 형식과 bake 재구성이 커서 보류.

**7. 손 튠 (`f7c4923`)** — 위에서 보면 손목 링과 8각형의 `thumb out`/`pinky out` 기둥이 살에서 멀었다. 손목 폭은 손목 평면 ±½중수골 안의 살에서, 바깥 기둥은 **손 살 전체**의 `s` 극값에서 잡히므로 벌어진 엄지·새끼 끝의 폭이 그대로 온다. 여유 튠 4개(`wrist thumb`/`wrist pinky`, `thumb out`/`pinky out`, 양손 공통). 측정 창을 기둥 근처 살로 좁히는 것은 값을 보고 결정.

### 작업 방식
- 지난 세션과 같다(슬라이더 → 표). 토폴로지는 매번 파이썬으로 재현해 닫힘·Euler 검사(열린 간선 = 손목 사각형 2개만이면 손이 닫음), 컴파일은 `dotnet build`. containment·self-collision·deform은 에디터에서 사람이.

### 씬에 저장된 튠 값 (2026-08-30 종료 시점, 씬 단위 m)

| 대상 | 값 |
|---|---|
| arm `hi` / `lo` | 0.002 / 0 |
| arm `outward hi` / `outward lo` | −0.05 / 0.02 |
| arm hi `front` / `back` | 0.03 / 0.02 |
| arm lo `front` / `back` | 0.055 / 0.02 |
| head `tilt` / `offset` | 25° / 0.023 |
| head `front` / `back` | 0.005 / −0.04 |
| crown `front` / `back` | 0.01 / 0.004 |
| spine `front` / `back` | 0.01 / 0 |
| spine1 · spine2 `front` / `back` | 0 / 0 (미튠) |
| `crotch drop` / `hip out` | 0.08 / 0.8 |
| pelvis `front` / `back` | 0 / 0.006 |
| knee `out` / `back` | 0.005 / 0.02 |
| ankle `tilt` / `front` / `back` | 45° / −0.08 / 0.005 |
| `delt along` / `delt up` | 0.4 / 0 (미튠) |
| elbow `hi` | 0.01 |
| `wrist thumb` / `wrist pinky` | −0.016 / −0.004 |
| `thumb out` / `pinky out` | −0.01 / −0.01 |

전부 "튠 중" — 표로 옮긴 값은 아직 없다. 고정값에서 풀린 것: knee `back` 0.1 → 0.02, elbow `hi` 0.05 → 0.01.

### 다음 단계 (세션 종료 시 정한 순서)
1. **길이 편집 스윕.** 실제로 각 뼈를 늘이고 줄여 보며 원하는 대로 따라오는지, `check containment`·`check self-collision`이 깨지지 않는지 검사. 이 세션의 구조 변경(골반·발·척추·어깨·대칭 삼각화)은 전부 rest에서만 봤다.
2. **튠 값 표로 이관.** 두 세션 분 슬라이더 값을 cage.md §3·§3c~e·§4 표와 recipe 리터럴로 옮기고 슬라이더 정리. head splitter 오브젝트 삭제.
3. **크기·두께 driver.** 머리 크기, 몸통·팔다리 두께처럼 rest 살 측정값에 절대 여유만 더하는 단면들이 길이를 따라 커지게 하는 선언 — 링/기둥마다 "이 단면을 구동하는 뼈(또는 전신 척도)"를 열로 두고 `단면 = rest 단면 × f(driver 길이 / rest)`. §4c endbone이 선례(cage.md §9 첫 항목).

### 남긴 일 (세부)
- ankle의 측정 창이 joint slab(종아리 뼈 × 0.25)이라 정강이·발볼 살까지 단면에 들어온다 — 헐거우면 전용 창. toe·tip 위쪽·폭에는 여유 열 없음. tip 위끝이 발가락 살 전체의 up 최대라 뚜껑이 발볼 높이.
- 골반 판 깊이가 Hips·UpLeg 살 전체 구간이라 crotch 정점도 엉덩이 깊이 — 안쪽 허벅지 벽이 헐거우면 crotch만의 깊이로.
- `L/R hip` 높이는 `hip out` 하나로 옆·위가 함께 정해진다 — 따로 필요하면 up 오프셋 열.
- spine1·spine2 링 튠(배·아랫가슴 깊이), spine2와 arm·lo의 높이 순서 확인.
- `delt` 튠(along·up); 삼각근 살이 LeftShoulder 본이면 살 창 넓히기; delt 링 앞/뒤 여유 열 없음.
- 손 바깥 기둥·손목 폭의 측정 창을 기둥 근처 살로 좁힐지 결정.
- 이전 항목 그대로: 승모근 능선 측정 창, `neck mid` 깊이, 자기겹침 스윕 자동화 — cage.md §9.

---

## 2026-09-01 — 길이 스윕

커밋: 없음(작업 트리). 코드 [cage.cs](../unity/Assets/Scenes/cage.cs)·[mapping_tester.cs](../unity/Assets/Scenes/mapping_tester.cs), 신규 [tools/cage_sweep](../tools/cage_sweep), 문서 [cage.md](cage.md) §7b.

### 시작 상태
- 236 정점 / 468 삼각형. 지난 세션이 남긴 순서의 1번이 "길이 편집 스윕" — 골반·발·척추·어깨·대칭 삼각화를 전부 rest에서만 봤다.
- 검사는 인스펙터 버튼 둘뿐이라 사람이 한 케이스씩 눌러야 했다. 그리고 `check containment`는 **LBS로 스킨한 메시**를 쟀다 — 코드 주석이 스스로 "케이지 기반 변형이 생기기 전까지는"이라 밝힌 잔재이고, `cage_deform`이 생긴 뒤로 만료돼 있었다.

### 결정

**1. 몸은 LBS가 아니라 사상된 메시다.** 이 파이프라인에 스키닝은 없다 — 스켈레톤이 케이지를 몰고 케이지가 메시를 몬다. `find_outside`의 LBS 경로와 `skinned()`를 걷어내고 순수 `outside(pts, cage, tris)`로 좁혔다. 그러면 containment도 `[N8]`대로 **길이만의 함수**가 되어 rest 밖에서도 의미가 있고, Transform 의존이 사라져 에디터 없이 돈다.

**2. 파이썬 재구현이 아니라 소스째 컴파일.** `cage.cs`·`cage_deform.cs`가 UnityEngine에서 쓰는 것은 `Vector3`·`Mathf`·`Debug.Assert`뿐이고 셋 다 `CoreModule`의 순수 관리 코드라 엔진 없이 돈다 — 스파이크로 확인했다. 그래서 스윕 csproj가 두 파일을 `<Compile Include>`로 링크한다. 재구현이 없으니 두께 드라이버가 `cage_ring`에 열을 더해도 갈라질 제2 구현이 없다. 같은 이유로 내보내기는 리플렉션 JSON이다(손으로 쓴 바이너리 리더/라이터 쌍은 필드가 늘 때 조용히 어긋난다).
- 성능도 이쪽이 낫다: 케이스당 370 ms 직렬 → 24코어에서 30 ms. Unity Mono 단일 스레드로는 몇 시간짜리다.
- 에디터가 프로젝트를 열고 있으면 Library가 잠겨 배치 모드가 막힌다. 그래서 내보내기는 인스펙터 버튼이 1차, `-executeMethod mapping_tester.export_headless`가 2차.

**3. 층으로 쌓은 케이스.** 53본 전조합은 2⁵³이라 열거할 수 없다. 본 하나씩 8단(424), 본 쌍의 네 모서리(5,512), 전신 무작위(20,000). 실제로 일어나지 않는 조합도 남겼다 — 인구를 모형화하는 게 아니라 레시피가 깨지는 자리를 찾는 것이다.

**4. `--skip`.** 손가락을 풀어 두면 3층이 99.9% 실패한다(무작위 전신이면 53본 중 손가락 하나쯤은 반드시 위험 구간에 든다). 몸통에 대해 아무 말도 하지 않으므로 이름 조각으로 본을 rest에 묶는 손잡이를 뒀다.

### 측정 (25,936 전신 + 21,196 몸통, seed 1)

**rest 기준선 0 / 36,426 정점 탈출, 0 삼각형 자기겹침.** §9의 "어깨·배·팔 위아래가 판을 뚫는다"는 만료됐다 — 제어점이 40 → 236으로 늘면서 해소됐고, 그 항목을 측정값으로 고쳤다. 덕분에 판정이 "기준선 대비"가 아니라 **절대 0 기준**이다.

몸통 23본 중 **18본이 0.5–1.5 전 구간 무사**하다. 걸린 다섯은 전부 골반이다.

| 본 | 안전 범위 | 첫 실패 |
|---|---|---|
| pelvis | 0.75 – 1.5 | 0.625: `L/R hip+crotch+spine mid`, `spine+spine1` 4 삼각형 |
| L/R hip | 0.625 – 1.5 | 0.5: Hips 9 + UpLeg 6 정점 탈출 |
| L/R thigh | 0.5 – 1.25 | 1.375: Hips 7 정점 탈출 |

손은 훨씬 좁다 — `index 1`·`pinky 1`이 0.875–1.125, `ring 1`이 1–1.5(줄이면 바로 깨진다), `middle 1` 0.875–1.5, `thumb 3` 0.625–1.375. 나머지 손가락 마디는 전 구간 무사. 즉 **깨지는 것은 첫 마디(중수골 쪽)뿐**이고, 분기 링을 나누는 손바닥 8각형 기둥들이 서로를 관통한다.

**쌍 상호작용은 거의 없다.** 각각 홀로는 깨끗한 3,286쌍 중 함께 두어야만 깨지는 것은 **75쌍**뿐이고, 거의 전부 `clavicle=0.5`가 한쪽이다: `chest=0.5 + clavicle=0.5` → `L arm+L delt`·`L arm+head`·`crown mid+head+head mid` 7 삼각형. 쇄골을 줄이면 어깨 쐐기가 머리·목 판을 관통한다 — `[N11]`의 `arm outward hi = −0.05`가 arm 링 윗변을 승모근 위로 **절대 거리**만큼 당기는데, 쇄골이 짧아지면 링이 이미 정중선 쪽에 와 있어 그 5 cm가 `neck mid`를 넘는다. 비율이어야 할 값이 절대값이라 생기는 문제이고, §9 두께 driver와 같은 종류다.

전신 무작위(몸통만, 20,000): 13,158 실패 / 6,842 통과. 자기겹침 빈도 순으로 **머리·정수리**(`crown mid+head+head mid` 3,984 · `crown+head` 3,848) → **라글란 V·어깨 쐐기**(`L/R arm+head` 2,700 · `L/R arm+L/R delt` 2,400) → **허리**(`spine+spine1` 2,295) → **골반**(`L/R hip+crotch+spine mid` 1,675) → 고관절–무릎(1,040) → 발목–무릎(380). 탈출은 `LeftUpLeg` +444(`left hip=0.5 + right hip=0.5`), `Head` +387, `Hips` +158.

### 덧 — 씬 뷰 와이어 (같은 세션, 스윕 뒤)

커밋: `fdd1f70` → `acac1a1`(+`f19a21f`, `6d9859c`)

`Gizmos.DrawWireMesh`가 껍질의 702변을 전부 그려, 표에 없는 삼각화 대각선 232개가 판 안에 깔려 있었다. 레시피만 그리도록 `cage.frame`을 만들었다.

- **첫 판단: 저장하지 않는다.** 대각선을 숨기려면 wire 목록을 구워야 하는데(`tris`에서 복원 불가 — 판에서는 가로대와 대각선이 똑같이 hi↔hi다) 이득이 33%뿐이었다. 대신 링 사각형 + 기둥만 = 212선(−70%), 새 데이터 0. 코너 상수가 이미 사각형을 순환 순서로 돌아 인덱스 산술로 끝난다.
- **그 뷰는 틀렸다.** 고관절 링(`crotch`–`L hip`)이 보이지 않는다는 지적. 이 케이지의 분기 링은 **이름이 다른 기둥 둘**이다 — 제어점을 공유해야 하므로 링(정점 4)이 될 수 없었기 때문(`[N13]`·`[N15]`·`[N4]`). 어느 기둥이 짝인지는 §5 표에만 있고 `cage_constants`는 삼각화된 형태로만 갖고 있었다.
- **선 개수가 아니라 구조가 척도였다.** 실제 선택지는 "212선 불완전" vs "466선 완전"이었다. 저장하기로 뒤집었다.
- 다만 저장하는 것이 처음 계획과 다르다. 그 사이에 **`strip`의 가로대도 표에 없다**는 것을 알았다 — 사다리가 `v[i]`를 `v[j]`에 짝지은 것은 대각선과 똑같이 삼각화의 선택이다. 그래서 `cage_constants.grid`는 `strip`이 아니라 `plates`/`walls`에서 직접 나온다: 사슬의 연속 기둥 쌍. §5d에 불변식으로 적었다.
- 측정: grid 306변(전부 껍질 위), 전체 frame 466 / 702. 껍질의 702 = 선언된 450 + 대각선 232 + 사다리 가로대 20. 가로대가 20뿐인 것은 벽 쿼드에서 가로대가 외곽선과 겹치고 긴 판(몸통 10점 등)에만 내부 가로대가 생기기 때문.
- `all_edges` 토글로 삼각화 전체로 되돌아간다 — 포함·자기겹침은 표면에 관한 것이다. `OnDrawGizmosSelected`를 `#if UNITY_EDITOR`로 감쌌다.

### 남긴 일
- **골반이 1순위.** 단독 편집으로 깨지는 유일한 부위다. §9 "골반 다듬기 (2) 골반 판 깊이가 Hips·UpLeg 살 전체 구간이라 crotch 정점도 엉덩이 깊이"가 가리키는 자리와 같다.
- **`arm outward hi`를 비율로.** 쇄골 길이에 대한 비율이면 쌍 상호작용 75쌍이 대부분 사라질 것으로 보인다.
- **손 첫 마디.** 손바닥 8각형 기둥이 첫 마디 길이를 따라가지 않는다 — `[N6]`의 endbone 수법을 분기 링에도.
- 스윕은 튠·레시피가 바뀔 때마다 다시 돌린다. 두께 driver가 들어오면 `export sweep data`를 다시 누르는 것 외에 도구 수정은 없어야 한다 — 그것이 이 도구의 설계 목표다.
- 여전히 이전 세션 그대로: 튠 값 표로 이관, head splitter 삭제, 승모근 능선 측정 창, `neck mid` 깊이.

---

## 2026-09-02 — 골반 V링

커밋: `cage.cs` + `docs` + `scene`(이 항목과 함께).

### 시작 상태
스윕(§7b)이 단독 편집으로 깨지는 유일한 부위로 골반을 지목했다: `pelvis` 0.75–1.5, `L/R hip` 0.625–1.5, `L/R thigh` 0.5–1.25. 나머지 몸통 18본은 전 구간 무사.

### 진단 (수치로 확인)
`spine` 링은 `n = up`, `along = 0`이라 두 변이 Spine 관절 높이에 **정확히** 놓인다. 반면 `L/R hip` 기둥은 `1.8·UpLeg − 0.8·Hips`라 pelvis 뼈(Hips→Spine)에 **움직이지 않는다**. 그래서 pelvis를 줄이면 링이 고관절 아래로 가라앉고 골반 판이 위로 접혀 몸통 판을 뚫는다.

| pelvis | spine ring y | hip y | |
|---|---|---|---|
| 0.750 | 0.00076 | 0.00064 | OK |
| 0.625 | 0.00063 | 0.00064 | **교차** |
| 0.500 | 0.00051 | 0.00064 | 교차 |

교차 시작점이 스윕이 보고한 첫 실패(0.625)와 일치한다.

### 결정 — 변별 걸림 `[N16]`
`cage_ring`에 `hold_hi`/`hold_lo`(기둥 인덱스). 변의 평면 = `max(자기 앵커 + 여유, 걸림 기둥의 n 좌표)`. 기둥 두 끝의 최댓값을 쓰므로 기울어진 기둥은 먼 끝으로 건다.
- `spine.hold_hi = L hip`, `hold_lo = R hip`. 표가 짝지은 그대로(§5d의 `spine·hi–L hip`).
- 정중선 기둥 `spine mid`는 걸리지 않고 계속 관절을 따른다 — rest에서 링과 같은 높이(0.00101)이므로, 옆변만 붙잡으면 **가로일자 → 평평 → 정중선을 바닥으로 하는 V**가 자동으로 생긴다. 고관절 기둥 자체는 손대지 않았다.
- **순서**: 기둥이 링보다 먼저 놓이도록 `control_points`를 바꿨다. `post_ends`는 관절 중심만 읽고 링 변은 기둥에 걸릴 수 있으므로 의존이 한 방향이고 순환이 불가능하다. bake의 `rest_corners`는 걸림이 붙기 전에 계산되므로 빈 기둥 배열을 받는다.
- **부작용 사전 검증**: clamp가 spine·hi를 spine1·hi 위로 밀면 몸통 판이 접힌다. 걸림 높이(고관절)는 pelvis·lumbar와 **무관한 상수**라 구조적으로 불가능하다 — 최악 pelvis 0.5·lumbar 0.5에서 여유 +0.00036. 3층 무작위 20,000 케이스가 실제로 확인했다.

### 측정 (몸통 21,196 케이스, seed 1, 전후 동일 조건)

| | 이전 | 지금 |
|---|---|---|
| 실패 | 13,407 | 13,069 |
| 자기겹침 | 7,069 | **6,315** (−11%) |
| 포함 | 11,340 | 11,126 |
| 1층 단독 | 8 / 184 | **6 / 184**, 자기겹침 **0** |
| 2층 쌍 | 241 | 204 |
| `pelvis` 안전 범위 | 0.75–1.5 | **0.5–1.5** |

자기겹침 그룹은 골반 계열만 사라지고 나머지는 **숫자 하나까지 동일**했다: `spine+spine1` 2295→0, `L hip+crotch+spine mid` 1695→0, `R hip+crotch+spine mid` 1654→0, `L/R hip+knee+crotch` 162·158→0. 변경이 의도한 것 하나만 했다는 증거.

### 남긴 일
- **자기겹침 1위가 머리로 옮겨갔다**: `crown mid+head+head mid` 3,984 · `crown+head` 3,848 · `crown+crown mid+head` 3,475. 그 다음이 라글란 V·어깨 쐐기(`L/R arm+head` 2,700, `L/R arm+L/R delt` 2,400). 머리 링 `[N12]`과 crown 캡 사이, 그리고 `[N11]`·`[N15]` 구간이다.
- **골반에 남은 것은 포함**이고 자기겹침이 아니다: `L/R hip=0.5`에서 +15(Hips 9, UpLeg 6), `L/R thigh=1.375`에서 +7(Hips 7). 전부 `Hips` 살이라 골반 판이 엉덩이를 덮지 못하는 건 — cage.md §9 골반 (2)(3) 계열.
- 걸림 높이가 `hip out`·`crotch drop`에 딸린다. 그 둘을 튠하면 스윕을 다시 돌린다.
- 쇄골 비국소성(`arm outward hi`를 비율로), 손 첫 마디 — 어제 항목 그대로.

---

## 2026-09-04 — 목을 조이고, 판정 기준을 바로잡다

커밋: `6100a69` → `6f99dda` → `7efa89b`(씬) → `e14a323` → `ec006a6` → `5561c9a` → `536838e`. 대안 브랜치 `try-face`(`ff1a87c`, `3e5b91e`).

### 시작 상태
236 정점 / 468 삼각형. 골반은 `[N16]`으로 닫혔고, 스윕이 지목한 1순위가 쇄골–턱 구간으로 옮겨와 있었다: 자기겹침 상위 7개 그룹이 전부 머리·목·어깨였고, 6,315 자기겹침 케이스 중 4,213이 그 구간 그룹만으로 이루어져 있었다.

에디터가 프로젝트를 열고 있어 배치 모드가 막히므로, 이 세션의 모든 측정은 `Assets`·`Packages`·`ProjectSettings`를 임시 폴더로 복사해 그쪽에서 `export_headless`를 돌린 결과다. 저장소는 건드리지 않는다.

### 진행

**1. 정중선 기둥의 앞끝을 띠에서 풀었다 (`6100a69`, `6f99dda`)** — 목·얼굴을 조이려면 arm 링의 hi `front`를 줄여야 하는데, 정중선 기둥은 자기 띠의 깊이를 그대로 쓰므로(§3b) V 바닥(v72)이 함께 뒤로 끌려 쇄골 사이로 파고들었다. rest에서 두 값이 같다(둘 다 0.1186)는 것이 원인 그 자체다. 앞끝에만 자기 여유를 준다 — `neck front`, 이어서 겨드랑이 쪽에 같은 이유로 `sternum front` `[N17]`. 뒤끝은 목 뒤·등이 띠와 같은 깊이라 그대로 뒀다.
- 검증: 사본을 `neck front` 0.03으로 다시 구워 236 정점 중 **v72 하나만** 0.03 앞으로 움직였고 토폴로지와 grid는 동일했다.
- 값: `arm hi front` −0.05에서 가슴이 rest에 70 정점 새고, `sternum front`를 0.01 → 0.02 → 0.03으로 밀면 40 → 10 → **0**이었다. 씬은 0.025로 정착.

**2. arm 링을 head 링 안쪽으로 (`7efa89b`)** — arm 링 윗변을 깊이 0.0210까지 당기면 head 링 앞변(0.0556)보다 뒤에 서므로, 목과 쇄골을 함께 줄여도 두 면이 서로를 스쳐 지난다. 문턱이 정확히 두 깊이가 교차하는 지점이다(3 cm 당김에서는 여전히 3 삼각형).
- 대가가 둘. rest 포함이 824 정점 깨졌고(얼굴 508 + 윗가슴 292 + 목 앞 24), **chest 뼈 단독**이 0 → 12 삼각형으로 나빠졌다. 앞의 것은 1번의 두 여유로 되돌렸다. 뒤의 것이 남았다.
- 얼굴·턱을 위해 crown 링을 head 링의 xz 발자국으로 바꾸고 face 링을 넣는 안을 설계까지 했으나(정점 236 → 242, 기둥 셋, 뒤통수 벽 교체), **`try-face` 브랜치로 남기고 클린 복귀**했다. 이유: 그 안은 얼굴 fit을 정면으로 고치지만 자기겹침의 원인인 "이음선이 목뼈를 따라가지 않는다"는 손대지 않는다.

**3. 목 판의 주름을 턱으로 (`e14a323`)** — 남은 `chest=0.5`는 목 판 쿼드가 비틀리는 것이었다. 대각선을 바꾸면 주름이 "이음선→턱끝"에서 "턱 옆코너→V 바닥"으로 옮겨간다. 새 기구는 필요 없었다 — 사다리가 고리의 `v[0]`을 `v[2]`에 잇기 때문에, 같은 고리를 한 칸 돌려 추적하면 winding과 가로대는 그대로이고 대각선만 반대 코너로 간다 `[N3]`.
- 먼저 베이크된 `tris`를 직접 갈아 결과를 확인하고(닫힘·방향·부피 통과), 그 다음 표를 고쳤다. 사본을 구워 오른쪽 앞면이 정확히 `(v16,v67,v72) + (v67,v70,v72)`로 나왔다.
- 1·2층: 자기겹침 50 → **15** 케이스, `chest=0.5` 단독 4 → 0. 포함은 206 → 262 케이스로 나빠졌는데 전부 2~16 정점 규모다.

**4. "걸림"은 보류** — arm 링 윗변에 턱선 상한을 두는 안(`[N16]`의 `s`축 거울상)을 구현·측정까지 했다: 1·2층 자기겹침 50 → **3** 케이스, 손으로 고른 케이스 전부 0. 그러나 "턱이 어깨를 내린다"는 원격 인과가 납득되지 않아 최후의 수단으로 남기고 되돌렸다. 측정값은 여기 남긴다.

### 판정 기준을 바로잡았다 (`ec006a6`, `5561c9a`, `536838e`)

"포함 검사를 애초에 왜 시작했는가"를 되짚었다. 원래 예상은 "rest 케이지 밖 정점은 변형 케이지로 올바르게 사상되지 않는다"였다. 재보니 절반만 맞았다 `[N18]`.

- **좌표는 밖에서도 유효하다.** MVC의 정의식은 안이라는 조건을 쓰지 않는다. 케이지를 정확한 아핀 변환으로 옮기면 6 cm 밖의 점도 0.33 mm 안에서 따라온다. 실제 길이 편집에서도 1 cm 밖 표본의 이탈이 중앙 0.00 cm, 95번째 0.08 cm였다.
- **다만 조건수가 나빠진다.** 음수 가중치 31% → 63%, Σ|w| 1 → 7. 그래서 rest 포함은 지켜야 한다.
- **면 위는 안 된다.** 면에 얹힌 정점은 커널의 특수 분기가 빗나가 최악 8 mm. 0.1 mm에서 5.9 mm, **0.5 mm**에서 평평(최악 0.076 mm). 메시 정점이 이산적이라 마침 얹히는 정점이 없을 수도 있지만 그건 운이고, 튠을 한 틱 옮길 때마다 그 한 경우를 사람이 볼 수는 없다. 그래서 규칙은 **여유를 갖고 담긴다**이고 검사가 그것을 잰다 — `clearance` 0.0005, 광선 패리티와 최근접 면 거리를 한 패스에서.
- **변형 후 이탈은 보장할 수 없고 보장할 필요도 없다.** 오목한 케이지에서 가중치가 음수이므로 안의 점이 안에 남는다는 보장이 없다(`양 고관절 0.5`에서 444 정점). 그러나 좌표는 bind에 고정되므로 나갔다는 것이 잘못 사상됐다는 뜻이 아니다. 스윕은 자기겹침만 판정하고 이탈은 분포로 싣는다.

같은 20,000 케이스가 13,224 실패 → **5,277**로 읽힌다. **단독 편집으로 깨지는 본이 하나도 없다.**

### 새 기준으로 본 현재 상태

rest 여유가 **310 정점** 부족하다. 거의 전부 손이다 — 손가락 마디 전부가 0.045~0.47 mm이고, 가장 가까운 것이 `index1`·`middle1`·`pinky1`의 0.045 mm. 몸통에서는 머리 판 0.260 mm(`crown mid+head+head mid`)와 골반 판 0.298 mm(`L hip+crotch+spine mid`)뿐이고 나머지는 0.9 mm 이상이다.

원인은 룰이다. **`margin`이 비율이라 절대 여유가 부위 크기에 비례한다** — 반폭 7 mm 손가락에 5%는 0.35 mm뿐이고, 둥근 단면을 감싼 사각 링은 변의 중앙에서 가장 가깝다. 몸통은 같은 5%가 센티미터를 준다.

### 씬에 저장된 튠 값 (2026-09-04 종료 시점, 씬 단위 m)

바뀐 것만. 나머지는 어제와 같다.

| 대상 | 값 |
|---|---|
| arm hi `front` | 0.03 → **−0.05** |
| `neck front` | **0.05** (신설) |
| `sternum front` | **0.025** (신설) |

### 남긴 일
- **`inflate`에 절대 하한.** §2에 값을 두고 `max(비율, 하한)`으로 부풀려 손가락을 한계값 위로 올린다. 머리·골반은 튠이 판을 살에 붙인 것이라 그 값을 다시 본다.
- **남은 자기겹침의 정체 확인.** 전신 순위가 머리(`crown+head` 3,036 · `head+head mid+neck mid` 2,073) → 허리(`spine+spine mid+spine1` 1,670) → 어깨 쐐기(`L/R arm+L/R delt` 1,354~1,380) → 가슴 띠(`L/R arm+sternum mid` 1,283~1,324) → 고관절–무릎(1,023~1,058)이다. 골반 V와 쇄골–목처럼 하나씩 기전을 짚어야 한다.
- **걸림**(`[N16]`의 `s`축 거울상)은 측정을 남긴 채 보류. 머리 그룹이 계속 1위로 남으면 다시 꺼낸다.
- 여전히 이전 세션 그대로: head splitter 삭제, 승모근 능선 측정 창, `arm outward hi`를 비율로, 손 첫 마디.

---

## 2026-09-05 — 맞추는 대신 지킨다: 첫 보정(gate)

커밋: (이 세션). main으로 돌아와서 — `try-neck-fit`은 측정 지점으로 남겨 둔다.

### 방향 전환

케이지의 모양은 **주어진 메시에 의존적**이다. 망치상어처럼 머리가 좌우로 긴 모델이나 어깨에 돌출부가 있는 모델을 주면 "살에 꼭 맞춰 자기겹침을 없앤다"는 목표 자체가 도달 불가능해진다 — 어떤 메시든 여유를 빼앗아 갈 수 있다. 그래서 fit을 더 정교하게 하는 대신 **비상식적인 스켈레톤 변화에서 케이지를 지키는 gate**를 둔다 `[N20]`. 그런 변화가 실제로 요구되면 그때 케이지를 고도화해도 늦지 않다.

gate는 케이지의 한 부분을 다른 부분에 비추어 판단하므로, 스켈레톤 → 케이지 평가가 **전부 끝난 뒤** 보정으로 돈다(§6b). 링이 기둥에 걸리는 것(`[N16]`)은 기둥이 먼저 놓인다는 한 방향 의존으로 평가 중에 풀렸지만 링 대 링에는 그런 순서가 없다. 보정이 늘면 서로가 옮긴 것을 다시 옮기므로 **표 순서를 우선순위로** 정해 둔다.

### 첫 보정 — `head above arms`

머리는 어깨 위에 있다. 목을 줄이면 파팅 평면의 최하단(기울기 때문에 앞 두 코너 v64·v67)이 arm 링 hi 변(v4·v5·v16·v17) 아래로 내려가고, 이음선이 턱선을 넘어 목 판이 머리 판을 뚫는다(§9 지도 A). 부족분만큼 머리 뭉치 — crown 링, head 링, `crown mid`, `head mid`의 정점 12 — 를 `up`으로 통째로 올린다. 뭉치가 함께 움직이므로 박스의 모양은 그대로다.

- `[N16]`의 거울상(arm 링 윗변에 턱선 상한)은 "턱이 어깨를 내린다"는 원격 인과라 되돌렸었는데(2026-09-04, 측정만 남김), 이쪽은 **"어깨가 머리를 올린다"**로 인과가 바로 선다. 같은 자리를 반대쪽에서 잡는다.
- 바닥은 **양쪽** arm 링의 hi 변 넷 전부의 max로 잡았다 — 한쪽 쇄골만 편집하면 좌우 높이가 달라지므로. 올리는 기준은 head 링 네 코너의 min이라 tilt를 0으로 바꿔도 성립한다.

### 측정 (헤드리스 재굽기 → 보정 없는 케이지와 나란히)

| 케이스 | 들린 높이 | head 최하단 − arm 최상단 | 보정 없이 | 보정 후 |
|---|---|---|---|---|
| rest | **0.0 mm** | +21.3 mm | 0 | 0 |
| chest 0.5 | 43.6 mm | 0.0 mm | 0 | 0 |
| 아랫목 0.5 | 1.3 mm | 0.0 mm | 0 | 0 |
| 윗목 0.5 | 1.0 mm | 0.0 mm | 0 | 0 |
| **chest + 아랫목** | 66.2 mm | 0.0 mm | **8** | **0** |
| **chest + 아랫목 + 윗목** | 88.6 mm | 0.0 mm | **10** | **0** |
| chest + 목 + 양쪽 쇄골 | 88.6 mm | 0.0 mm | 22 | 10 |

**rest는 정확히 0 움직임**이다 — 분기 없이 no-op이 성립한다. 걸린 케이스는 전부 `head 최하단 − arm 최상단 = 0.0 mm`, 즉 접촉까지만 올리고 멈춘다. 목뼈 셋을 다 줄이면 머리가 **8.9 cm** 올라가는데, 그것이 `[N20]`이 말하는 "살이 뼈에서 떨어지는" 양의 실측이다.

### 1·2층 스윕 (손 rest 고정, 1,196 케이스, seed 1)

**실패 7건**(단독 0, 쌍 7), rest 기준선 **310 정점 밖 / 자기겹침 0**.

| 케이스 | 삼각형 | 그룹 |
|---|---|---|
| `chest 0.5 + L/R 쇄골 0.5` | 5 | `L arm+L delt` · `L arm+sternum mid` · `head+head mid+neck mid` |
| `어깨밑동 1.5 + L/R 쇄골 0.5` | 2 | `L arm+L delt` · `head+head mid+neck mid` |
| `pelvis 0.5 + L/R 고관절 0.5` | 2 | `spine+spine mid+spine1` · `hip+knee` |
| `L 고관절 0.5 + R 고관절 0.5` | 16 | `knee+crotch` · `ankle+knee` |

이력: 50 → 15 → **7**.

### 이것이 말하는 것

`try-neck-fit`이 폭 knob으로 얻은 것도 7건이었고 **케이스 목록이 정확히 같다**. 그런데 그쪽은 rest 포함 **1,410** 정점(얼굴 514·귀 590 포함)을 내주고 얻은 값이고, 이쪽은 **310**(손 304 + Hips 2 + …)으로 **머리 포함이 그대로**다. 삼각형 수도 더 낮다 — 쇄골 쌍 10 → 5, 어깨밑동 쌍 3 → 2. 같은 자리를 fit으로 미는 것보다 gate로 지키는 쪽이 싸고 정확했다.

따라서 **머리 박스 재설계는 당분간 필요 없다.** chest–목 축소의 자가충돌이 gate로 닫혔고, 이 줄기에는 얼굴·귀 containment 문제가 애초에 없다. 남은 7건은 쇄골 넷과 골반·다리 셋뿐이고 목·턱과 무관하다.

### 여유(slack) — 목이 짧아지게 둔다

닿는 즉시 올리면 목뼈를 줄인 만큼 머리가 그대로 따라 올라와 **편집이 상쇄된다**(목뼈 셋 0.5에서 88.6 mm). 그래서 재는 곳은 최하단으로 두되 바닥 아래로 얼마까지 봐줄지를 **여유**로 뺀다.

- 처음 시험한 것은 "최하단 대신 중간지점"이었는데 한 칸 지나쳤다 — 중간에서 `chest+아랫목`이 4 삼각형으로 되살아난다. 앞변 가중 `w`로 훑으니 **`w` 0.6까지 깨끗하고 0.5에서 깨진다**.
- 가중치를 정점 목록으로 표현하려면 인덱스를 반복해 넣어야 해서 표에 적기 나쁘다. head 링의 앞뒤 높이차가 96 mm이므로 같은 것을 **씬 단위 여유**로 적을 수 있다: 24 mm ≡ `w` 0.75, 38 mm ≡ `w` 0.60.

| 여유 | chest+아랫목 | chest+아랫목+윗목 | 아랫목+윗목 | chest 단독 | chest+목+쇄골 | 목 3개 축소 시 들리는 높이 |
|---|---|---|---|---|---|---|
| 보정 없음 | 8 | 10 | 0 | 0 | 22 | — |
| 0 mm | 0 | 0 | 0 | 0 | 10 | 88.6 mm |
| 24 mm | 0 | 0 | 0 | 0 | 10 | 64.6 mm |
| **38 mm** | **0** | **0** | **0** | **0** | 14 | **50.6 mm** |
| 45 mm | 0 | 0 | 0 | 0 | 14 | 43.6 mm |
| 50 mm | 4 | 4 | 0 | 0 | 14 | 38.6 mm |

**38 mm로 정했다.** chest+목 계열이 전부 0이고 경계(50 mm) 안쪽으로 12 mm 남는다. 쇄골 케이스가 10 → 14로 가지만 그것은 이 gate가 노린 자리가 아니라 원래 있던 것이고, **다른 gate로 따로 잡는다**. rest 이동량은 여전히 정확히 0.000 mm.

튠 슬라이더 `head gate slack`(0.038, 0..0.1)으로 두었다 — 값이 확정되면 §6b 표로 옮긴다.

### 전신 3층 (체크포인트 `512d81a`, 여유 0)

**2,837 / 20,000**(직전 5,277, `try-neck-fit` 4,376). 기전별로 갈라 보면 gate가 어디를 닫았는지가 분명하다.

| 그룹 | 2026-09-04 | try-neck-fit (fit) | **gate** |
|---|---|---|---|
| `crown+head` (A) | 3,036 | 2,031 | **0** |
| `head+head mid+neck mid` (A) | 2,073 | 1,754 | **822** |
| crown 계열 (E) | 422 | 1,451·1,445 | **0** |
| `L/R arm+sternum mid` (B) | 1,283·1,324 | 961·1,006 | **207·206** |
| `L/R arm+neck mid+sternum mid` (B) | 833·838 | 890·916 | **0** |
| `L/R arm+L/R delt` (C) | 1,354·1,380 | 1,137·1,175 | **381·421** |
| `spine+spine mid+spine1` (D) | 1,670 | 1,672 | 1,670 |
| `L/R hip+L/R knee` (D) | 1,023·1,058 | 1,024·1,059 | 1,023·1,058 |
| `ankle+knee` · `knee+crotch` (D) | 762 · 484 | 764 · 486 | 762 · 484 |

- **A는 거의 사라졌다** — `crown+head` 3,036 → 0, 나머지 −60%. **E는 0**이 되어 기전 자체가 없어졌다(머리 판이 더 이상 안장이 되지 않는다).
- **B −84%, C −71%.** 셋 다 목이 가라앉으면서 생기던 것이라 gate 하나가 함께 닫았다.
- **D는 정확히 그대로**(1,670 · 1,023 · 1,058 · 762 · 484). 골반을 건드리지 않았으므로 이것이 비교가 같은 조건이라는 확인이고, 동시에 **이제 D가 남은 것의 대부분**이라는 뜻이다.

### 다음에 볼 자기겹침

1·2층 1,196 케이스에 남은 **7건**. 목·턱은 닫혔고, 남은 것은 두 무리다.

**쇄골 무리 (4건)** — `chest 0.5 + L/R 쇄골 0.5`(7 삼각형), `어깨밑동 1.5 + L/R 쇄골 0.5`(3). 걸리는 그룹이 `L/R arm+L/R delt`(지도 C), `L/R arm+sternum mid`(B), `head+head mid+neck mid`·`crown+head`(A)로 넷에 걸쳐 있지만 방아쇠는 하나다 — **쇄골**. 뿌리도 이미 안다: `[N11]`의 `arm outward hi = −0.05`가 arm 링 윗변을 승모근 위로 **절대 거리**만큼 당기는데, 쇄골이 짧아지면 링이 이미 정중선 쪽에 와 있어 그 5 cm가 `neck mid`를 넘는다(2026-09-01 기록, §9 두께 driver와 같은 종류: 비율이어야 할 값이 절대값이다). **다음 gate의 대상**이고, 이번 여유 38 mm가 이 무리의 삼각형을 5 → 7, 2 → 3으로 올렸다 — 값을 되돌리는 대신 그 gate로 잡는다.

**골반·다리 무리 (3건)** — `pelvis 0.5 + L/R 고관절 0.5`(2), `L 고관절 0.5 + R 고관절 0.5`(16). 그룹은 `spine+spine mid+spine1`, `L/R hip+L/R knee`, `knee+crotch`, `ankle+knee`로 §9 지도 **D** 그대로다. 고관절 기둥만 뼈를 (1+f)배로 따라가고 spine 링의 변과 무릎 링은 따라가지 않는다 — `[N16]`이 **높이**는 닫았지만 **폭**은 열려 있다(`[N16]` 말미). gate로 잡을 수 있는지(무릎 링 안쪽 변이 crotch를 넘지 않게 하는 식) 아니면 두께 driver와 함께 볼지를 정해야 한다.

**전신 3층을 여유 38 mm로는 아직 재지 않았다.** 위 표는 여유 0(체크포인트)의 것이다 — 1·2층에서 쇄골 무리의 삼각형만 올라갔으므로 전신도 A·C 근처가 조금 오를 것으로 보이지만, 확인해야 한다. 그때 §9 지도도 함께 갱신한다.
